react-d3-component:
  cache:
    paths:
      - node_modules
  script:
    - npm install 
    - npm run styleguide:build
    - mv styleguide d3v7-chart 
  artifacts:
    paths:
      - d3v7-chart
  only:
    - d3v7
